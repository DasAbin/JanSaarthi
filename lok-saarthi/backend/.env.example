# JanSaarthi Backend Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED: LLM API Keys (Gemini primary, OpenAI fallback for 429 avoidance)
# ===========================================
# Get your free Gemini key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Required for fallback when Gemini quota exceeded (429)
# Get key: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Force a Gemini model if you get 404 (e.g. gemini-1.5-flash-latest, gemini-1.5-flash-8b)
# GEMINI_MODEL=gemini-1.5-flash-latest

# Optional: OpenAI fallback model when Gemini fails (o3-mini or gpt-4o-mini; placeholder key = fallback skipped)
OPENAI_FALLBACK_MODEL=o3-mini

# ===========================================
# OPTIONAL: Google Cloud TTS
# ===========================================
# Path to your Google Cloud service account JSON file
# Only needed if you want server-side TTS (otherwise browser TTS is used)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# ===========================================
# OPTIONAL: OCR Engine Selection (PaddleOCR fast mode for speed)
# ===========================================
# Options: "paddleocr" (faster) or "gemini"
# PaddleOCR requires Python + paddle dependencies installed
OCR_ENGINE=paddleocr
# Fast mode: skip angle classifier for faster OCR
OCR_FAST=true

# Path to Python executable (for PaddleOCR)
# PYTHON=python

# ===========================================
# OPTIONAL: Multilingual TTS (Piper) and STT (Vosk)
# ===========================================
# Piper TTS: path to piper binary (or "piper" if in PATH)
# PIPER_PATH=piper
# Directory containing per-language model folders (e.g. hi/model.onnx, en/model.onnx)
# PIPER_MODEL_DIR=storage/voices
# Vosk STT: path to unzipped model (e.g. vosk-model-small-hi-0.22)
# VOSK_MODEL=path/to/vosk-model-small-hi-0.22

# ===========================================
# Server Configuration
# ===========================================
PORT=4000
