### LokSaarthi API Notes

- Back-end base URL: `http://localhost:4000`
- All endpoints are JSON except file uploads (multipart/form-data).

See the repository `README.md` for the full API table and example payloads.

