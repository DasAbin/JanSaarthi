# Server
PORT=4000

# -------- LLM Providers (pick one) --------

# Option 1 (recommended on AWS): Bedrock (no external API keys)
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0

# Option 2: Gemini
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-1.5-flash

# Option 3: OpenAI fallback (optional)
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_FALLBACK_MODEL=o3-mini

# -------- OCR --------
# paddleocr uses Python script if available; otherwise the app falls back to pdf-parse + tesseract.js automatically.
OCR_ENGINE=paddleocr
OCR_FAST=true

# Optional: specify python executable (if using PaddleOCR/Silero/Indic TTS scripts)
PYTHON=python

