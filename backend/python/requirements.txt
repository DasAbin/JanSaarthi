paddleocr
paddlepaddle
pypdfium2

# Indic-Parler-TTS (AI4Bharat) - Free, local TTS for Indian languages
# Install parler-tts from GitHub:
git+https://github.com/huggingface/parler-tts.git
transformers>=4.30.0
soundfile>=0.12.0
numpy>=1.24.0  # Silero STT
# Note: Silero models loaded via torch.hub (no separate package coilneeded)
torch>=2.0.0
torchaudio>=2.0.0